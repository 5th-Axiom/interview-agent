# 项目约定

## 依据与范围

- 先读 `docs/prd.md`、`docs/technical-plan.md` 和 `docs/implementation-brief.md`。
- 最新用户要求优先。页面参考图与正文冲突时，以最新正文为准。
- 项目在本目录直接开发；参考仓库仅供阅读，不能修改：`/Users/circle/git/alice-a2a` 与 `/Users/circle/project/github/aural-oss`。
- 本地文档于 2026-09-10 从飞书同步；变更产品行为时保持实现与文档一致。

## 实现原则

- React、TypeScript、Next.js；常驻语音 Relay 和后台 Worker；PostgreSQL 与私有对象存储。
- 前端仅分 Foundations、基础组件、业务组件、页面四层。状态逻辑用 Hook，请求放功能目录的 api.ts，不增加 Flutter 式层级。
- H5 与 PC 共用交互逻辑；Token、主题、组件有明确边界，支持浅色和深色。
- 面试提问、追问、语义选岗和自然收尾主要由 Prompt 决定；工程承担鉴权、校验、可靠保存、计时、取消和有限重试。
- 全场只保留一个逻辑上下文；滚动摘要有来源和版本，完整原始事件保留，不能按题目清空历史。
- 控制命令、保存、重面资格消费和后台任务必须可安全重试。任何音频或模型异步回调都要隔离旧连接与旧回复。
- 不把本地演示数据或模拟语音当成真实供应商实现。测试模式明确标识，生产缺少配置时明确报错。

## 安全与验证

- 密钥、手机号原文、私有音频与访问令牌不提交 Git，不打印到日志。
- 测试固定验证码只允许非生产且显式开启，生产启动时拒绝不安全配置。
- 新增数据库及对象存储使用本项目独立实例或命名空间，不能清理其他项目数据。
- 复用 aural-oss 代码保留其 MIT 版权和许可。
- 对关键会话、压缩、音频取消、保存重试、鉴权和重面审核写有实际价值的测试；做真实浏览器的 H5 / PC 检查。
- 完成后如实说明可运行功能、测试结果、实际供应商联调状态和仍缺的环境配置。
