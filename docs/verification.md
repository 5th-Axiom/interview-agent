# 实现与验证记录

更新：2026-09-11。依据 PRD、技术方案、实施说明及本轮“自己启动服务，从候选人与控制台用户视角验证并修复”的要求。

## 本轮完整用户流程复验

- 自行运行 Web、Relay、Worker 及独立 PostgreSQL / MinIO；先在明确测试模式跑协议回归，再恢复真实供应商模式。
- Chrome PC / 390px H5 全流程通过，业务写操作都从页面触发：招聘登录 → 新建草稿 → 真实试聊 → 发布 / 复制入口 → 候选人登录 / 试音 / 选岗 → 真实 ASR / LLM / TTS 播放 → 暂停 / 刷新续接 / 结束确认 → 反馈评分 / 标签 / 真实语音转写 / 提交 → 申请重面 → 记录筛选 / 实际播放录音 / 保存评估要求 / 真实重新生成 / 人工意见 → 拒绝与说明 / 再申请 / 通过 → 一次资格开启新一场 → 草稿与发布隔离 / 停用。页面无 JavaScript 异常、PC / H5 无横向溢出。
- 实际发现并修复：保存发言后的 350ms 待回复窗口被声音检测取消后无法恢复；恢复页面未连麦时结束按钮无效；审核通过仍受旧入口缓存影响而返回旧反馈页；新会话不能回退使用旧入口会话判断。
- 补齐记录返回 / 刷新后的筛选与页码保留、无匹配记录的清空入口、人工意见离开提醒和当前标签页草稿恢复、审核确认与申请时间、岗位读取失败重试和不存在状态。语音控制失败保留恢复入口且不会冒出未处理的 Promise 异常。
- 反馈评分和标签改为正确的表单分组，单个问题标签具有独立可访问名称；真实录音无有效转写时不再误称测试模式；已消费的重面资格引导查看最新面试。
- 复查真实对话时补齐正式面试的工具边界：已有岗位直接开始交流；正式阶段仅开放结束相关控制，不能调用选岗工具或编造岗位开放状态。真实模型复验能直接围绕 React / TypeScript 项目提问。
- Chrome 故障注入通过：岗位加载失败重试、评估保存失败及后台刷新保留输入、取消还原、浏览器后退 / 前进恢复未保存人工意见、错误验证码不丢手机号、拒绝麦克风后可重试、不存在会话提供正确恢复入口。
- 私有面试录音及反馈语音均在 Chrome 实际播放并推进播放时间；非法 Range 返回 416 及 Content-Range。对应记录为 `.local/media-result.json`。
- 结果为 `.local/journey-result.json`、`.local/recovery-result.json` 和 `journey-*.png`。验收生成的岗位已停用，面试证据保留；合成麦克风输入有明确标识，不代表真人 / 手机实机验收。

## 本轮修复

- 反馈的前后端统一为最多 500 个 Unicode 字符，超限保留输入；问题标签与 PRD 一致；空反馈不能提交；跳过会确认放弃并不保存原内容；支持取消录音、失败重试转写；重面申请原因恢复为选填。反馈提交幂等且不会被迟到的重复提交覆盖。
- 麦克风授权、试音、反馈录音和岗位选择的异步返回增加退出隔离；错误后进入可恢复状态；旧回复完成事件不再修改新回复状态。模糊选岗可播报澄清，保留短期选岗历史。
- 按用户授权仅复制 LLM / ASR / TTS 凭据及对应配置到 `.env.local`（权限 0600、Git 忽略）；没有复制参考项目数据库、Supabase、存储或账号权限。参考仓库保持只读。
- 新增 DashScope ASR 和 TokenDance TTS 适配，真实等待 ASR task-started；流式 PCM 按 500ms 发送，保留网络半采样点，播放队列施加背压。测试登录与语音模式独立，每场固定真实 / 测试模式。
- ASR 轮换排空旧连接，修訂一条合并发言中的片段时保留其他片段；噪音打断后没有有效文字时由服务端接续；错误、取消、有限重试和连接代次贯穿语音链路。鉴权错误不当作临时故障重复请求供应商。
- 文本、音频和播放回执支持去重 / 重试；结束后的 30 秒收尾窗口仅接受本人当前连接在结束前采集的数据。尾部待识别音频由独立后台任务补转写，不触发实时对答；空识别结果作为音频核对事件，不假装候选人发言。
- 评估等待 30 秒尾片接收窗口和补转写任务完成后执行，避免晚到音频遗漏；所有评估证据链接都能定位可见的原文或辅助播放记录。
- 播放证据按生成句子的 event_id 关联，不能因同一轮其他句子的回执而误标已播放。上下文聚合播放回执，原始流水不变；超长单次发言可生成带来源的临时摘要视图。
- 修复摘要任务过早空跑一次后不再调度的问题；作业增加租约 token，领取、续租、结果写回和重试均隔离过期执行者；重复崩溃同样受三次预算限制；到期扫描独立运行。
- 评估要求支持仅保存、取消修改、生成前确认和版本冲突校验。失败 / 生成中继续显示旧草稿，人工意见保留。岗位保存增加乐观版本校验。
- 新建岗位尚未保存时，试聊不会在正式岗位列表产生草稿；试聊使用本组织招聘入口，关闭后资源释放，失败提供恢复提示。
- 面试记录按 50 条分页；岗位筛选覆盖历史片段；日期按上海时区解释并校验；保留筛选条件与加载重试。

## 验证结果

| 检查               | 结果与边界                                                                                                                                                                               |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 自动测试           | 23 项通过：真实独立 PostgreSQL 事务、并发开场、资格消费、幂等、鉴权、OTP、连续压缩、评估隔离、供应商协议、分阶段工具权限、PCM 分包、租约过期、反馈边界、录音尾片权限                     |
| HTTP 业务边界      | 通过：空反馈 / 501 字拒绝、跳过不保存内容、已提交反馈不被重复修改、申请原因选填、历史岗位筛选、分页、非法日期、评估设置 CAS、旧岗位保存拒绝、未保存试聊不污染岗位列表                    |
| PC Chrome          | 登录、选岗自动开场、回答持久化、暂停 / 续接、刷新恢复、结束、反馈、重面审核、人工意见、录音加载通过；无页面 JavaScript 错误                                                              |
| H5 Chrome 390px    | AudioWorklet 44.1k → 16k、试音、测试语义选岗、静音保留、主题不打断、换岗保留会话、离线续接、试聊隔离通过；无横向溢出或页面 JavaScript 错误                                               |
| Relay / 私有录音   | 伪造票据、打断后旧音频隔离、重复回执、音频重试、显式接管、旧连接拒绝、结束通知通过；无登录不能访问录音，Range 返回 206 且读取指定 100 字节                                               |
| 真实供应商请求     | DeepSeek 中文流式回复与结构化 JSON、TokenDance 24kHz 语音、DashScope 中文夹 React / TypeScript 的短音频识别通过；输入为合成测试句                                                        |
| 真实上下文与选岗   | 真实模型连续三次压缩后保留 1800ms 原始基线、纠正后的 850ms 和缓存待讨论话题，31 条原始事件不丢失；明确选岗返回正确工具 ID，模糊意图不擅自选岗；真实语音经 HTTP 选岗接口正确选中前端岗位  |
| 真实浏览器语音链路 | Chrome 390px 使用合成 WAV 作为麦克风：真实 ASR 发言、LLM 回复、TTS 浏览器播放及回执、结束后真实评估与私有录音通过；无页面 JavaScript 错误。录音因有被取消 / 未播完片段正确标记为部分缺失 |
| UI 边界            | 通过：501 字禁用提交、星级评分、取消麦克风、跳过确认保留输入、放弃后内容不提交、评估取消、生成确认取消不创建新版本、所有证据锚点存在、H5 无溢出                                          |
| 视觉检查           | 本轮检查 PC 反馈 / 详情及 H5 真实语音 / 记录截图；沿用既有 Token 与主题。原始空事件文案和评估待生成状态已修正                                                                            |
| 构建与类型         | `npm run typecheck`、Next.js 生产构建通过                                                                                                                                                |
| 生产配置保护       | 固定验证码或测试语音开启时生产启动拒绝；本地联调不等于正式部署                                                                                                                           |

供应商协议测试覆盖：SSE 分字节与 UTF-8、工具与朗读文本分离、主备共用上下文、已输出后不重播、用户取消不重试、401 不重试、相邻 JSON / SSE 音频拼接、半个 PCM 采样点、ASR 就绪前缓冲、最终文本去重、合并发言修订与连接轮换。

运行记录保存在 Git 忽略的 `.local/`：`provider-check.json`、`context-live-check.json`、`live-result.json`、浏览器结果及截图。它们不是公开候选人数据，也不包含供应商密钥。

## 当前可运行方式

当前 `.env.local` 使用真实语音供应商，`ALLOW_TEST_OTP=true` 单独开启本地测试登录。`npm run dev:all` 运行 Web 3100、Relay 3101 和 Worker；PostgreSQL / MinIO 使用本项目独立命名空间。若需要离线回归，停止当前进程后运行 `npm run dev:test`，再运行 `test:browser`、`test:mobile`、`test:relay`。`npm test` 始终使用独立测试数据库和明确替身，不因本地联调配置而调用真实供应商。

## 外部条件和未作出的承诺

- 没有找到可用的真实短信服务与模板配置；当前没有发送真实短信。短信 HTTP 网关已实现，正式接入需要 `SMS_URL` / `SMS_TOKEN` 及供应商签名和模板。
- 招聘方当前为单组织口令工作台，具备服务端组织权限与口令哈希；未接客户 SSO / OIDC 和成员目录。
- 本轮使用真实 Chrome、供应商网络与合成语音。没有冒用真人音频，没有完成 iOS Safari / Android 实机、扬声器回声、蓝牙、真实弱网及长时间多实例容量验收。
- 端到端单次通过不能证明 P95 首声 2 秒或停声 300ms。最新单次记录：LLM 完整短回复约 1.10 秒，TTS 首个可播放分片约 2.56 秒，TTS 总耗时约 3.93 秒，短音频 ASR 约 1.32 秒。当前样本没有达到端到端首声 2 秒目标；记录见 provider-check.json，不作为生产 SLA。
- 上下文有字符预算、带来源压缩、最近原文和超长单轮视图；仍未采用所选模型的精确 Token tokenizer，也未形成大量场次的长期事实漂移基准。
- 原始音频仍以 200ms 分片保存，便于验证可靠性；尚未据此宣称大规模存储吞吐或成本目标。实时 Relay 有连接数、速率和队列上限，后台有低并发租约作业；容量上限需要目标并发与部署地域后实测。
- 正式域名 / 证书发布、数据保留政策、自动删除、备份恢复演练和成本运营看板不在本地供应商联调的通过结论内。未对其他项目或现有记录执行清理。
